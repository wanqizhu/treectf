def grade(arg, key):
	if "labCTF{bee_there_or_bee_square}" == key:
		return True, "I know it was a bad idea to let the bees escape..."
	else:
		return False, "Incorrect. Make sure your flag is in the default format."

